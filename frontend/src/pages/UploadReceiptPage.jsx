import { useState } from "react";
import { api } from "../api";

export default function UploadReceiptPage() {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>Subir Boleta (OCR)</h1>
    </div>
  );
}
